# 第 2 课修改对比

状态：用户确认后已应用到[正式第 2 课](../../chapters/02-Agent运行时.md)，包含第 5 节图文；未提交、未发布。[候选稿](02-Agent运行时-候选稿.md)保留为审阅记录，后续正文以正式文件为准。应用时只修正相对图片与资料链接。

## 改写前的判断

原稿的四组件分工、Harness 控制点、环境差异和排查表都有保留价值。本轮不推翻这些内容，主要修正与已应用第 1 课的衔接，并把责任分类转成可操作的排查过程。

- 原稿开头已经揭晓“程序却在等网络文件系统返回”，读者尚未看见证据如何支持这个结论。
- 第 1 课已经建立订单排查助手，第 2 课却重新切到“读取 config.json，告诉我当前主题”。候选改为同一助手读取 server.log。
- LangChain／LangGraph 插在 Harness 的执行控制中间，会打断主线；保留其定位，放入独立的框架小节。
- 原稿已有小标题和表格，不重犯第 1 课首版候选取消结构的问题。专业术语继续保留，解释紧邻具体对象。

## 结构与内容对比

| 原稿 | 候选调整 | 读者增加的判断能力 |
| --- | --- | --- |
| 一次读取，四种职责 | Runtime 组成：Model、Harness、Tool、Environment | 把抽象职责对应到模型请求、宿主代码、读取函数和文件环境 |
| Harness 在几个关键位置作决定 | Harness：输入组织、执行控制与结果回传 | 区分“模型写出申请”和“程序允许执行”，找到应检查的控制点 |
| Tool 与 Environment 怎样区分 | Tool 与 Environment：代码实现与运行条件 | 不把路径处理错误、文件未挂载和系统权限拒绝混为模型问题 |
| 最后才展示排查记录 | 故障定位：沿执行证据缩小范围 | 从已确认完成的步骤向后查；缺少返回记录不能直接证明网络阻塞或死锁 |
| 框架说明混在 Harness 段落中 | 框架定位：LangChain 与 LangGraph | 看清框架代劳的职责，以及应用仍须提供的工具和约束 |

保留章标题，不更名、不改课号。候选不重复第 1 课的 Workflow／Agent 选型，也不提前展开第 3 课的完整消息协议和循环代码。

## 核心修正示例

原稿开场提前给出结论：

> 排查后才发现：模型早已交出读取申请，程序却在等网络文件系统返回。

候选先让读者检查同一次调用的记录：模型返回了什么、工具是否找到、调用入口是否通过、哪里还没有结果。随后分别讨论“实际读取仍在等待”和“记录缺失”的可能，不把缺日志写成已经查明根因。

这是对证据边界的补充，不是新增一次真实事故。文中的卡住、未挂载和权限拒绝都明确为教学情境；本轮没有制造挂起进程，也没有运行沙盒或真实模型实验。

## 资料与范围

### 第 5 节配图融合

用户反馈只看框架定义仍不清楚用途。本次保留专业标题，复用已经生成的小黑图，并将原来的概述段落改为“LangChain：Agent Loop 的现成实现”和“LangGraph：任务编排与状态管理”两个小节。前者用读取日志、读取源码解释循环代码如何复用；后者用未来修复助手的测试与返工说明步骤、分支和状态。

配图放在本节开头，使用仓库内相对路径引用，不重新生图。图中的“现成循环装在底座上”说明 LangChain Agent 基于 LangGraph；文字同时保留 LangGraph 可独立使用、LangChain 也支持定制与持久化的边界。示意流程不作为实际模型运行证据。

图文融合版本已获用户确认并应用，接下来只准备第 3 课候选与对比，正式第 3 课仍须另行确认。

沿用原稿的 OpenCode 固定提交 `50efc055de282e0e54a87ccebb8e2054cc45efd2` 与[既有核验记录](../../research/01-05-chapter-promotion-sources.md)，本轮未重新审计这些固定源码。

2026-09-10 通过 agent-reach 的 Jina 网页读取通道复核了 [LangChain overview](https://docs.langchain.com/oss/python/langchain/overview) 与 [LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview)：官方仍说明 LangChain 提供模型／工具接口和 Agent 入口、其 Agent 基于 LangGraph；LangGraph 可独立使用，并提供有状态编排、持久化和人工介入能力。候选据此保留原有定位，不展开新产品、安装步骤或 API 示例。

用户确认前只更新候选、对比、配图引用记录与改写进度；确认后应用第 2 课正式正文。可执行代码、目录及已有第 1 课改动保持不变；不提交、不发布。

## 编辑检查

按用户确认的标准执行：专业标题与术语、清晰小节、必要对照、节内通俗解释。`ljg-writes` 组织递进，`ljg-plain` 解释难点，Sepia 技术文章检查负责发现重复、跳跃和未经证据支持的措辞。Markdown 的章节结构按书籍需要保留。

原稿作者模型未知；执行者为 GPT，版本未知。作者 prose layer 为 none，执行者为 prior。以用户已接受的第 1 课和原稿中有效的结构为偏好参照，没有独立核验的人工文风样本，不据此宣称去除了 AI 痕迹。

验收重点：读者能否解释四种职责，并根据证据选择下一步检查位置。候选写完不等于这一阅读效果已经验证。
