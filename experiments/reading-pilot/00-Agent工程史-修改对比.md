# 第 0 课修改对比

状态：已应用到[正式第 0 课](../../chapters/00-Agent工程史.md)，本轮未提交或发布。[候选稿](00-Agent工程史-候选稿.md)保留为审阅存档，仅为正式文件调整了相对链接；以下保留应用前的修改对比与验证记录。

## 改写前的判断

历史篇已有清楚的 Auto-GPT 开场和已核验节点，不需要另造故事。这次沿用五节框架，让每组历史工作对应一个明确的工程问题，并接到已完成改写的课程上。

- 保留“Auto 却要人工敲回车”的开场，收紧为早期 Auto-GPT 的具体事实，不把它扩大成整个 Agent 领域的起点。
- 小节标题保留年份与专业名称，正文分别区分 ReAct 的交互方式、Toolformer 的训练方法与结构化调用接口，不把三者写成同一种框架。
- 保留 SWE-bench 与 SWE-agent 的对照，将落点放在任务验收和工具反馈；历史节不重讲完整 Tool Calling Loop。
- 将 MemGPT 与 LangGraph 对应的两个问题说清：本轮看什么，与程序从哪里恢复。保留邮件回执反例，不把保存状态说成确认外部副作用。
- 协议节只讲 MCP 和 A2A 当时解决的连接问题，不把后来规范字段倒写进首发历史。章末用工程责任与课次对照，补上第 10 课的编排位置。

## 内容对比

| 位置 | 原稿重点 | 候选调整 |
| --- | --- | --- |
| 开场 | Auto-GPT 从 Prompt 到人工授权循环 | 保留事件与语气，明确只聚焦大语言模型参与后的若干节点 |
| 第 1 节 | 模型开始使用外部结果 | Tool Use 与结构化调用：分别说明交互、训练、接口 |
| 第 2 节 | 代码任务需要真实结果 | Benchmark 与操作界面：试卷与作答系统不能混同 |
| 第 3 节 | 长历史与可恢复状态 | Context 与持久化：选择输入、恢复状态、确认副作用各有职责 |
| 第 4 节 | 连接标准化 | MCP 与 A2A：保留协议范围，不展开版本迁移教程 |
| 第 5 节 | 新能力带来新责任 | 对照本书第 1～10 课，明确这是一种工程归纳，不是历史上的统一升级顺序 |

历史篇保持独立选读，不强行套用订单排查的连续剧情。保留原引用集，不新增产品排行榜、最新版本表、运行代码或插图。

## 事实与验证边界

本轮逐项对照[现有历史资料核验](../../research/00-agent-engineering-history-sources.md)，尤其是 2026-09-09 的补充记录。沿用已核验年份、论文固定版本与 Auto-GPT 提交；不把 2026-09-01 的项目快照称为当前最新状态。

保留的约束包括：最早 Auto-GPT 提交只有 Prompt；早期命令循环仍需人工授权；ReAct／Toolformer 不提供完整运行时；SWE-bench 不是 SWE-agent；Checkpoint 不证明外部动作只发生一次；协议连接不代替权限和执行控制。

核验候选与正式稿的外部引用集合、编号、本地链接和 Markdown 结构。本轮不重新联网研究历史、不安装或执行历史项目，不把文字一致性检查当成新的源码运行验证。正式历史篇、SUMMARY、研究记录及三条待确认的旧重定向保持不变。

检查已通过：导读正式稿与已确认候选仅相对链接不同；历史候选保留原标题及原有 12 个外部引用链接，正文引用编号对应完整；本轮检查的六份文档共 32 个本地链接目标存在。历史事实按现有核验记录对照，不声称完成了新的网络复核。

## 编辑检查

`ljg-writes` 让历史节点围绕工程问题递进，`ljg-plain` 解释相邻概念的区别，Sepia 保留原稿有效开场并检查重复与跳跃。专业标题和 Markdown 分节不变；没有为历史文章编造测试代码。

原稿作者模型未知；执行者 GPT、版本未知；prose layer 为 none／prior。已确认章节作为风格偏好，不作为已核实的人类写作语料；最终阅读效果由作者验读。
