# 第 5 课修改对比

状态：用户要求继续推进后，已将本版应用到[正式第 5 课](../../chapters/05-上下文工程.md)，本轮未提交或发布。[候选稿](05-上下文工程-候选稿.md)保留为审阅记录，后续以正式正文为准；应用时仅调整相对链接和检查链接名称。此前第 1～4 课已包含在提交 `a07ecfc` 中。

## 改写前的判断

原稿的预算计算、完整轮次边界、最新摘要组装和缓存权衡都需要保留。本轮继续订单排查场景，减少构建日志、主题配置之间的切换，并把压缩失败时的处理单独说清。

- 第 4 课已经区分持久数据与当前 Context；本课直接回答“哪些历史进入这一轮”，不重新解释如何保存 Session。
- 原稿把 T3 的 User／Tool Call 标作未完成并保留，但需要补一句关键限制：保留这段状态不等于现在能提交模型请求。未完成的工具调用仍须取得对应结果。
- 压缩后的 Prompt View 属于会话输入部分，不包含全部固定说明和工具 Schema；算总预算时不能漏掉这些输入和输出预留。
- 给定摘要、教学 Token 数和手工日志必须明确标注，不能写成真实模型或 Tokenizer 的测量结果。

## 结构对比

| 原稿内容 | 候选组织 | 要保住的判断 |
| --- | --- | --- |
| 构建日志与裁剪 | Artifact 与 Prompt View：保留证据，选择输入 | 缩短返回内容不等于删除原始日志 |
| 什么时候压缩历史 | Token Budget 与 Compaction：先计量，再决定 | 总预算包括固定输入、新结果、输出预留；字节不等于 Token |
| 安全切点与摘要内容 | 压缩边界：完整 Turn 与工具消息配对 | 不拆 Tool Call／Result；未完成轮次不能靠截断变成完成 |
| 摘要保存后重建消息 | Prompt View：最新摘要、保留尾部与新增消息 | 写下压缩记录之后，必须刷新真正发出的输入 |
| Prompt Cache 成本 | 失败处理与 Prompt Cache：控制压缩代价 | 压缩失败不破坏旧记录；变短不保证更便宜或更准确 |

## 实现与验证范围

正文仍使用三个小代码片段：裁剪四行示例日志、计算给定的窗口预算、组装普通文本消息。它们解释当前机制，不新建框架、不调用真实摘要模型。配套检查现已切换为读取正式正文，候选归档不再作为检查对象。

额外检查复用现有实现：综合实践的完整 Turn 切点与视图重建，用于检查最近轮次、未完成调用和重复压缩；已有第 5 课参考实现的长度检查，用于验证更长摘要被拒绝时记录不变。二者策略不完全相同：参考实现还尝试 Split Turn，不能把它当作本课整轮策略的同一实现。本轮不修改这些已有代码。

测试检查的是切点、消息结构、原始记录与已知失败路径，不证明摘要语义完整、不测真实 Token、不证明模型更准确，也不测量服务商缓存费用。摘要的目标、约束、事实与未完成事项仍需按具体任务验收。

已运行 `python -B experiments/reading-pilot/check_lesson_05.py`：三个正文片段输出符合示例；完整轮次切点保留最近一轮和当前未完成调用，补齐结果前仍能识别待处理编号；二次压缩接住旧摘要，原始字节前缀保留，新增消息只追加一次。模拟摘要服务失败、空摘要、更长摘要均未改动已有文件。第 4 课正式正文检查也已重跑通过。

来源沿用原稿的 OpenClaw、Pi、Hermes 固定提交及[既有核验记录](../../research/01-05-chapter-promotion-sources.md)，保留服务端压缩资料作延伸阅读；本轮不宣称更新了这些项目的当前版本结论。

## 编辑检查

按已确认风格保留专业标题、分节、必要的表格和过程示意。`ljg-writes` 组织输入选择的递进，`ljg-plain` 只解释节内难点，Sepia 检查重复与跳跃，`ponytail` 要求复用已有实现并留下一个可运行检查。

原稿作者模型未知；执行者 GPT、版本未知，prose layer 分别为 none／prior。参照已确认的前四课和原稿中有效的例子，不把生成成功、结构检查通过或代码通过当成读者已经理解。
