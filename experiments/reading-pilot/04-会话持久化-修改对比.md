# 第 4 课修改对比

状态：用户确认后已应用到[正式第 4 课](../../chapters/04-会话持久化.md)，未提交、未发布。[候选稿](04-会话持久化-候选稿.md)保留为审阅记录，后续以正式正文为准。应用时仅修正相对资料路径及检查链接名称。

## 第 1～3 课连续检查

已连读三篇正式正文。第 1 课选择实现方式，第 2 课定位运行职责，第 3 课展开工具循环，均沿用订单排查助手，未发现需要再次修改已确认正文的衔接阻塞。第 1 课讲申请与执行的区别，第 2 课补充 Harness／Environment 的定位，第 3 课用代码落实；这部分递进保留。

需要第 4 课承接的是：第 3 课的完整 demo 已把消息写入文件，正文展示的精简循环才只保留内存消息。因此，第 4 课不能笼统说“之前没有持久化”。本轮从“记录已有，怎样识别并找回同一会话”开始，区分设计需求、存储示例和已经实现的功能。

本次连续检查是编辑与代码对应检查，不等于已验证读者的理解效果。前三篇不再改动。

## 原稿中保留与调整的部分

- 保留已经有效的三笔流水／余额快照例子，以及两种计算都得到 12 的代码。
- 保留 JSON 与 JSONL 的区别、Checkpoint 可以放进 JSONL 的解释，不为了减概念而统一扩展名。
- 把 Session 落到会话编号、目录和记录归属；Turn 与 Session 分开说明，不让读者把一个文件名当成定义。
- 原稿保存的示例状态只有摘要；候选补上前两条消息和已处理位置，让读者看见一份可指认的快照，但明确不提供完整崩溃恢复实现。
- SQLite 查询、unknown 状态归并和并发事务不在此处展开。原有第 6 课已讨论执行账本、状态表、唯一约束与事务，专项练习也保留。本课只交代存储选择的触发条件。

## 结构对比

| 原稿 | 候选调整 | 本节需要讲清什么 |
| --- | --- | --- |
| 以部署提醒在重启后遗忘开场 | 接续已保存的排查记录 | 写下记录之后，应用仍要识别旧会话并准备恢复材料 |
| Session 的归属说明 | Session：会话身份与存储边界 | 一个会话可以包含多轮，编号如何关联记录 |
| Transcript／Checkpoint 与账户例子 | 保留对比，先讲用途，再展示恢复位置 | 全量历史与某个位置的状态不是同一份信息 |
| JSON 快照只有摘要，JSONL 单独举例 | 用同一组短消息展示快照字段和 JSONL 记录 | 同一内容可以用不同格式组织，格式不决定用途 |
| 长期记忆与材料选择 | Memory 与 Context：跨会话信息和当次输入 | 新会话可以使用长期偏好，但不能无故继承旧进度 |
| 恢复边界、数据库迁移占两节 | 合为短的恢复边界与存储演进 | 旧快照不证明副作用没发生；数据库不是按行数升级 |

## 验证与边界

正文中的短对话和状态为明确的教学数据，不声称是第三课真实模型生成的记录。流水例子复用原稿；文件写入采用原有“临时文件 + replace”的单写者模式，只在临时目录中验证。不写用户 Memory，不调用模型，不实现新的恢复框架。

配套检查现已切换为读取正式正文，直接执行三个 Python 片段并核对输出、快照读回、JSONL 消息内容和新旧会话的材料隔离。它验证教学代码，不证明断电安全、并发安全或真实 Agent 已能跨进程续跑。

已运行 `python -B experiments/reading-pilot/check_lesson_04.py`：两种余额计算均为 12；快照写入和读回一致，位置为 2；JSONL 的两条消息与快照消息一致；新会话没有旧会话的等待进度，临时目录已清理。第 3 课正式正文检查也已重跑通过。

专业标题与结构按已确认风格保留：`ljg-writes` 理顺概念与动作的递进，`ljg-plain` 只解释节内难点，Sepia 检查重复与跳跃，`ponytail` 要求复用现有代码和标准库。原稿作者模型未知，执行者 GPT、版本未知，prose layer 分别为 none／prior；偏好参照为已确认的第 1～3 课与原稿中读者认可的例子，不声称具备经核验的人工文风样本。
